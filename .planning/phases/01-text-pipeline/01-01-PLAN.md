---
phase: 01-text-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - yt_to_skill/__init__.py
  - yt_to_skill/config.py
  - yt_to_skill/models/__init__.py
  - yt_to_skill/models/artifacts.py
  - yt_to_skill/models/extraction.py
  - yt_to_skill/stages/__init__.py
  - yt_to_skill/stages/base.py
  - yt_to_skill/llm/__init__.py
  - tests/__init__.py
  - tests/conftest.py
  - .env.example
  - .gitignore
autonomous: true
requirements:
  - INFR-02
  - EXTR-02
  - EXTR-03

must_haves:
  truths:
    - "Project installs cleanly with `uv sync` and all dependencies resolve"
    - "Pydantic extraction schema validates well-formed trading logic JSON"
    - "Pydantic extraction schema rejects malformed input with clear errors"
    - "REQUIRES_SPECIFICATION markers appear as null fields with paths collected in unspecified_params"
    - "Stage base protocol defines the artifact-guard contract all stages will follow"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata, dependencies, tool config"
      contains: "faster-whisper"
    - path: "yt_to_skill/config.py"
      provides: "PipelineConfig with OpenRouter key, model names, work dir"
      exports: ["PipelineConfig"]
    - path: "yt_to_skill/models/artifacts.py"
      provides: "VideoMetadata, TranscriptArtifact, FilterResult dataclasses"
      exports: ["VideoMetadata", "TranscriptArtifact", "FilterResult"]
    - path: "yt_to_skill/models/extraction.py"
      provides: "TradingLogicExtraction, StrategyObject, EntryCondition Pydantic models"
      exports: ["TradingLogicExtraction", "StrategyObject", "EntryCondition"]
    - path: "yt_to_skill/stages/base.py"
      provides: "Stage protocol with artifact guard pattern"
      exports: ["StageResult"]
  key_links:
    - from: "yt_to_skill/models/extraction.py"
      to: "yt_to_skill/models/artifacts.py"
      via: "shared types for pipeline data flow"
      pattern: "from.*models"
    - from: "yt_to_skill/config.py"
      to: ".env"
      via: "pydantic-settings loading env vars"
      pattern: "OPENROUTER_API_KEY"
---

<objective>
Create the project scaffold, dependency configuration, typed data models, and test infrastructure for the yt-to-skill pipeline.

Purpose: Every subsequent plan depends on these foundations — the Pydantic schemas define the data contracts between stages, the config defines how settings flow, and the stage base protocol defines the artifact-guard pattern all stages implement.

Output: A `uv sync`-able project with typed models, config, stage protocol, and pytest scaffold ready for implementation.
</objective>

<execution_context>
@/Users/criox4/.claude/get-shit-done/workflows/execute-plan.md
@/Users/criox4/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-text-pipeline/01-CONTEXT.md
@.planning/phases/01-text-pipeline/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffold, dependencies, and config</name>
  <files>pyproject.toml, yt_to_skill/__init__.py, yt_to_skill/config.py, yt_to_skill/llm/__init__.py, yt_to_skill/stages/__init__.py, yt_to_skill/models/__init__.py, .env.example, .gitignore, tests/__init__.py, tests/conftest.py</files>
  <behavior>
    - Test: PipelineConfig loads OPENROUTER_API_KEY from env vars
    - Test: PipelineConfig has default values for work_dir ("work"), translation_model, extraction_model
    - Test: PipelineConfig.work_dir returns a Path
    - Test: Missing OPENROUTER_API_KEY raises ValidationError
  </behavior>
  <action>
    1. Run `uv init` in the project root if pyproject.toml does not exist. Set Python requires to ">=3.11,<3.13" (ctranslate2 compatibility).

    2. Create pyproject.toml with:
       - name: "yt-to-skill"
       - Core deps: yt-dlp, youtube-transcript-api, faster-whisper, openai, instructor, pydantic, pydantic-settings, tenacity, loguru, langdetect
       - Dev deps: pytest, ruff, mypy
       - [tool.pytest.ini_options]: testpaths = ["tests"]
       - [tool.ruff]: target-version = "py311"

    3. Run `uv sync` to install all dependencies.

    4. Create yt_to_skill/config.py with PipelineConfig(BaseSettings):
       - openrouter_api_key: str (required, from OPENROUTER_API_KEY env var)
       - work_dir: Path = Path("work")
       - translation_model: str = "anthropic/claude-sonnet-4-20250514" (good for translation)
       - extraction_model: str = "anthropic/claude-sonnet-4-20250514" (good for structured output)
       - filter_model: str = "mistralai/mistral-7b-instruct" (cheap classification)
       - whisper_device: str = "cpu"
       - whisper_compute_type: str = "int8"
       - max_tokens_translation: int = 4096
       - max_tokens_extraction: int = 4096
       - model_config with env_file=".env"

    5. Create .env.example with placeholder: OPENROUTER_API_KEY=your-key-here

    6. Create .gitignore with: work/, .env, __pycache__/, *.pyc, .mypy_cache/, .ruff_cache/, dist/, *.egg-info/

    7. Create package __init__.py files for yt_to_skill/, yt_to_skill/models/, yt_to_skill/stages/, yt_to_skill/llm/.

    8. Create tests/__init__.py and tests/conftest.py with shared fixtures:
       - tmp_work_dir: creates a temp directory, yields it, cleans up
       - sample_video_id: returns "dQw4w9WgXcQ" (consistent test ID)
       - sample_transcript_segments: returns list of 5 segment dicts with start/end/text
       - mock_config: returns PipelineConfig with test values (mock API key)
  </action>
  <verify>
    <automated>cd /Users/criox4/Codes/yt-to-skill && uv sync && uv run pytest tests/ -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>Project installs with uv sync, config loads from env vars, pytest discovers and runs tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Data models — artifacts and extraction schema</name>
  <files>yt_to_skill/models/artifacts.py, yt_to_skill/models/extraction.py, yt_to_skill/stages/base.py, tests/test_models.py</files>
  <behavior>
    - Test: VideoMetadata can be created with video_id, title, duration, description, channel fields
    - Test: TranscriptArtifact validates method is Literal["captions", "whisper"]
    - Test: FilterResult has is_strategy bool and reason string
    - Test: EntryCondition with null value field produces REQUIRES_SPECIFICATION semantics
    - Test: StrategyObject.unspecified_params lists paths to null fields
    - Test: TradingLogicExtraction validates a well-formed JSON with strategies array
    - Test: TradingLogicExtraction rejects missing required fields (strategies, video_id)
    - Test: TradingLogicExtraction with null indicator values populates unspecified_params correctly
    - Test: StageResult dataclass holds stage_name, artifact_path, skipped flag
  </behavior>
  <action>
    1. Create yt_to_skill/models/artifacts.py with dataclasses:
       - VideoMetadata: video_id (str), title (str), description (str), duration_seconds (float), channel (str), upload_date (str | None), tags (list[str])
       - TranscriptArtifact: video_id (str), source_language (str), segments (list[dict] — each has start, end, text), method (Literal["captions", "whisper"]), caption_quality (Literal["good", "poor", "missing"])
       - FilterResult: video_id (str), is_strategy (bool), confidence (float), reason (str), metadata_pass (bool), transcript_pass (bool | None)
       Use dataclasses with dataclasses.asdict() for JSON serialization. Add to_json(path) and from_json(path) classmethods.

    2. Create yt_to_skill/models/extraction.py with Pydantic BaseModel classes:
       - EntryCondition: indicator (str), condition (str), value (str | None = None), timeframe (str | None = None), confirmation (str | None = None), raw_text (str)
       - StrategyObject: strategy_name (str), market_conditions (list[str]), entry_criteria (list[EntryCondition]), exit_criteria (list[EntryCondition]), indicators (list[str]), risk_rules (list[str]), unspecified_params (list[str])
         Add a model_validator that auto-populates unspecified_params by scanning entry_criteria and exit_criteria for None fields, building paths like "entry_criteria[0].value"
       - TradingLogicExtraction: strategies (list[StrategyObject]), video_id (str), source_language (str), is_strategy_content (bool = True)
         Add model_dump_json method override or to_file(path) helper

    3. Create yt_to_skill/stages/base.py:
       - StageResult dataclass: stage_name (str), artifact_path (Path), skipped (bool), error (str | None = None)
       - artifact_guard(output_path: Path) -> bool: returns True if artifact exists (cache hit)
       - Log message on cache hit via loguru

    4. Create tests/test_models.py implementing all behavior tests above.

    5. Update yt_to_skill/models/__init__.py to re-export key types.
  </action>
  <verify>
    <automated>cd /Users/criox4/Codes/yt-to-skill && uv run pytest tests/test_models.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>All model tests pass: extraction schema validates/rejects correctly, unspecified_params auto-populated from null fields, artifacts serialize to/from JSON, stage base protocol provides artifact_guard</done>
</task>

</tasks>

<verification>
- `uv sync` completes without errors
- `uv run pytest tests/ -x -v` shows all tests passing
- `uv run python -c "from yt_to_skill.models.extraction import TradingLogicExtraction; print('OK')"` succeeds
- `uv run python -c "from yt_to_skill.config import PipelineConfig; print('OK')"` succeeds (with OPENROUTER_API_KEY set)
</verification>

<success_criteria>
- Project scaffold installed and importable
- PipelineConfig loads from .env with typed validation
- All Pydantic extraction models defined with REQUIRES_SPECIFICATION semantics
- All artifact dataclasses defined with JSON serialization
- Stage base protocol with artifact_guard ready for stage implementations
- Test infrastructure with shared fixtures operational
</success_criteria>

<output>
After completion, create `.planning/phases/01-text-pipeline/01-01-SUMMARY.md`
</output>
