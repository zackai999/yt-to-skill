---
phase: 03-visual-enrichment
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - yt_to_skill/stages/skill.py
  - yt_to_skill/orchestrator.py
  - yt_to_skill/cli.py
  - tests/test_skill.py
  - tests/test_cli.py
  - tests/test_orchestrator.py
autonomous: false
requirements:
  - INPT-04

must_haves:
  truths:
    - "render_skill_md includes a ## Chart References gallery section when keyframes exist"
    - "render_skill_md omits gallery section when no keyframes are present"
    - "Gallery entries show timestamp and image link in format: **M:SS** -- ![](assets/keyframe_MMSS.png)"
    - "Orchestrator runs keyframe stage after skill stage when keyframes_enabled=True"
    - "Orchestrator skips keyframe stage when keyframes_enabled=False"
    - "Orchestrator re-renders SKILL.md with gallery after keyframe extraction"
    - "CLI --no-keyframes flag disables keyframe extraction"
    - "CLI --max-keyframes flag sets the cap"
    - "Keyframe PNGs are copied from work/<video_id>/keyframes/ to skills/<video_id>/assets/"
  artifacts:
    - path: "yt_to_skill/stages/skill.py"
      provides: "render_gallery_section and extended render_skill_md"
      exports: ["render_skill_md", "render_gallery_section"]
    - path: "yt_to_skill/orchestrator.py"
      provides: "Keyframe stage wired into pipeline"
      contains: "run_keyframes"
    - path: "yt_to_skill/cli.py"
      provides: "CLI flags for keyframe control"
      contains: "no-keyframes"
    - path: "tests/test_skill.py"
      provides: "Gallery section tests"
      contains: "TestGallerySection"
    - path: "tests/test_cli.py"
      provides: "CLI keyframe flag tests"
      contains: "TestNoKeyframesFlag"
  key_links:
    - from: "yt_to_skill/orchestrator.py"
      to: "yt_to_skill/stages/keyframe.py"
      via: "import run_keyframes"
      pattern: "from yt_to_skill\\.stages\\.keyframe import run_keyframes"
    - from: "yt_to_skill/orchestrator.py"
      to: "yt_to_skill/stages/skill.py"
      via: "re-render SKILL.md with gallery after keyframes"
      pattern: "run_skill.*force.*True"
    - from: "yt_to_skill/cli.py"
      to: "yt_to_skill/config.py"
      via: "keyframes_enabled and max_keyframes config override"
      pattern: "keyframes_enabled"
---

<objective>
Wire keyframe extraction into the pipeline: extend SKILL.md with gallery section, add keyframe stage to orchestrator, add CLI flags, and calibrate on real videos.

Purpose: Connects the standalone keyframe stage (from Plan 01) into the full pipeline so `yt-to-skill <url>` produces SKILL.md files with chart screenshot galleries.

Output: Extended `skill.py` (gallery rendering), wired `orchestrator.py` (keyframe stage + SKILL.md re-render), extended `cli.py` (--no-keyframes, --max-keyframes), tests for all, calibration checkpoint.
</objective>

<execution_context>
@/Users/criox4/.claude/get-shit-done/workflows/execute-plan.md
@/Users/criox4/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visual-enrichment/03-CONTEXT.md
@.planning/phases/03-visual-enrichment/03-RESEARCH.md
@.planning/phases/03-visual-enrichment/03-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan depends on -->

From yt_to_skill/stages/keyframe.py (created in Plan 01):
```python
def run_keyframes(video_id: str, work_dir: Path, config: PipelineConfig) -> StageResult:
    """Run keyframe extraction: download video, detect scenes, dedup, write PNGs.
    Sentinel: work_dir/video_id/keyframes.done
    PNGs: work_dir/video_id/keyframes/keyframe_MMSS.png
    """

def deduplicate_frames(frame_paths: list[Path], threshold: int = 10) -> list[Path]:
    """Remove near-duplicate frames using perceptual hash."""

def timecode_to_filename(timecode) -> str:
    """Convert FrameTimecode to keyframe_MMSS.png."""
```

From yt_to_skill/config.py (extended in Plan 01):
```python
class PipelineConfig(BaseSettings):
    # ... existing fields ...
    max_keyframes: int = 20
    keyframes_enabled: bool = True
```

From yt_to_skill/stages/skill.py (current):
```python
def render_skill_md(extraction: TradingLogicExtraction) -> str:
    """Render SKILL.md from extraction. Returns full markdown string."""

def run_skill(video_id: str, work_dir: Path, skills_dir: Path, *, force: bool = False) -> StageResult:
    """Generate SKILL.md. Creates assets/, scripts/, references/ scaffold."""
```

From yt_to_skill/orchestrator.py (current):
```python
def run_pipeline(video_id: str, config: PipelineConfig, *, force: bool = False) -> list[StageResult]:
    """Stages: ingest -> transcript -> filter -> translate -> extract -> skill"""
```

From yt_to_skill/cli.py (current):
```python
def main() -> None:
    """CLI with argparse: url, --output-dir, --force, --verbose"""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gallery section in skill.py and pipeline integration</name>
  <files>yt_to_skill/stages/skill.py, yt_to_skill/orchestrator.py, yt_to_skill/cli.py, tests/test_skill.py, tests/test_cli.py, tests/test_orchestrator.py</files>
  <behavior>
    - render_gallery_section([Path("keyframe_0142.png"), Path("keyframe_0310.png")]) returns "## Chart References\n\n**1:42** -- ![](assets/keyframe_0142.png)\n**3:10** -- ![](assets/keyframe_0310.png)"
    - render_gallery_section([]) returns ""
    - render_skill_md(extraction, keyframe_paths=[...]) appends gallery section at bottom
    - render_skill_md(extraction) with no keyframe_paths arg produces same output as before (backward compatible)
    - run_pipeline with keyframes_enabled=True runs keyframe stage after skill stage, copies PNGs to assets/, re-renders SKILL.md with gallery
    - run_pipeline with keyframes_enabled=False skips keyframe stage entirely
    - CLI --no-keyframes sets config.keyframes_enabled=False
    - CLI --max-keyframes N sets config.max_keyframes=N
    - TestGallerySection: test_renders_gallery, test_empty_returns_empty, test_gallery_in_full_render, test_no_gallery_by_default
    - TestNoKeyframesFlag: test_flag_parsed, test_flag_disables_keyframes
    - TestMaxKeyframesFlag: test_flag_parsed, test_flag_sets_cap
  </behavior>
  <action>
    1. Add `render_gallery_section(keyframe_paths: list[Path]) -> str` to skill.py:
       - If not keyframe_paths: return ""
       - Build lines starting with "\n## Chart References\n"
       - For each path in sorted(keyframe_paths): extract MMSS from stem "keyframe_MMSS", compute minutes=int(raw[:2]), seconds=int(raw[2:]), format as `**{minutes}:{seconds:02d}** -- ![](assets/{path.name})`
       - Return "\n".join(lines)

    2. Extend `render_skill_md` signature to accept optional `keyframe_paths: list[Path] | None = None`:
       - After building body, if keyframe_paths: append `render_gallery_section(keyframe_paths)`
       - Backward compatible: existing callers pass no keyframe_paths, gallery is omitted

    3. Extend orchestrator.py `run_pipeline()`:
       - Import `run_keyframes` from stages.keyframe
       - Import `shutil` for PNG copying
       - After the skill stage block (Stage 6), add Stage 7: Keyframe extraction (gated by `config.keyframes_enabled`):
         a. Run `run_keyframes(video_id, work_dir, config)` -- wrapped in try/except like other stages
         b. If keyframe result has no error: copy PNGs from `work_dir/video_id/keyframes/*.png` to `skills_dir/video_id/assets/`
         c. Collect keyframe paths from `skills_dir/video_id/assets/keyframe_*.png`
         d. If keyframe_paths non-empty: re-render SKILL.md by calling `run_skill(video_id, work_dir, skills_dir, force=True)` -- but first, extend `run_skill` to accept `keyframe_paths` and forward to `render_skill_md`
       - Log stage status same as other stages

    4. Extend `run_skill` signature to accept optional `keyframe_paths: list[Path] | None = None` and forward to `render_skill_md(extraction, keyframe_paths=keyframe_paths)`.

    5. Extend cli.py argparse:
       - Add `--no-keyframes` flag: `action="store_true"`, help="Skip keyframe extraction"
       - Add `--max-keyframes` flag: `type=int, default=None, metavar="N"`, help="Maximum keyframes per video (default: 20)"
       - After config creation: if `args.no_keyframes`: `config = config.model_copy(update={"keyframes_enabled": False})`
       - If `args.max_keyframes is not None`: `config = config.model_copy(update={"max_keyframes": args.max_keyframes})`

    6. Extend tests:
       - tests/test_skill.py: Add TestGallerySection class with test_renders_gallery (2 keyframes -> correct output), test_empty_returns_empty, test_gallery_in_full_render (render_skill_md with keyframe_paths includes gallery), test_no_gallery_by_default (render_skill_md without keyframe_paths has no gallery section)
       - tests/test_cli.py: Add TestNoKeyframesFlag with test parsing --no-keyframes sets config, and TestMaxKeyframesFlag with test parsing --max-keyframes sets config. Use unittest.mock.patch on run_pipeline to capture the config passed.
       - tests/test_orchestrator.py: Add test that when keyframes_enabled=True, run_keyframes is called; when False, it's skipped. Mock run_keyframes and verify call/no-call.

    Write tests first (RED), then implement (GREEN).

    IMPORTANT: The orchestrator keyframe stage error should NOT abort the pipeline -- keyframes are a nice-to-have. Log the error and continue, matching the established error handling pattern for translate/extract stages.
  </action>
  <verify>
    <automated>cd /Users/criox4/Codes/yt-to-skill && python -m pytest tests/test_skill.py tests/test_cli.py tests/test_orchestrator.py tests/test_keyframe.py -x -v 2>&1 | tail -50</automated>
  </verify>
  <done>Gallery renders correctly in SKILL.md. Orchestrator runs keyframe stage after skill, copies PNGs to assets, re-renders with gallery. CLI flags --no-keyframes and --max-keyframes work. All tests pass including existing test suite.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Calibrate AdaptiveDetector threshold on real trading videos</name>
  <files>yt_to_skill/stages/keyframe.py</files>
  <action>
    Human verifies the complete keyframe extraction pipeline on real Trader Feng Ge trading videos. The AdaptiveDetector threshold (currently 8.0) may need adjustment based on observed results. If threshold needs tuning, Claude adjusts the value in keyframe.py and re-runs tests.
  </action>
  <what-built>
    Complete keyframe extraction pipeline: `yt-to-skill <url>` now downloads video at 720p, extracts keyframes via PySceneDetect AdaptiveDetector (threshold=8.0), deduplicates with perceptual hashing, writes timestamped PNGs to assets/, and renders a Chart References gallery in SKILL.md.
  </what-built>
  <how-to-verify>
    1. Install new dependencies: `cd /Users/criox4/Codes/yt-to-skill && uv pip install -e ".[dev]"`
    2. Run on 2-3 Trader Feng Ge videos (Chinese crypto trading screen recordings):
       ```
       yt-to-skill https://www.youtube.com/watch?v=<VIDEO_ID_1> --verbose
       yt-to-skill https://www.youtube.com/watch?v=<VIDEO_ID_2> --verbose
       ```
    3. Inspect output:
       - Check `skills/<video_id>/assets/` -- count PNGs, verify they are distinct chart screenshots (not duplicates, not blank frames)
       - Check `skills/<video_id>/SKILL.md` -- verify ## Chart References section has timestamp links
       - Expected: 5-15 keyframes per video (not 0, not 20 cap-limited every time)
    4. If threshold too sensitive (hitting 20 cap on most videos): threshold needs to increase
    5. If threshold too conservative (0-2 frames): threshold needs to decrease
    6. Test --no-keyframes: `yt-to-skill <url> --no-keyframes` -- verify no keyframes extracted
    7. Run full test suite: `python -m pytest tests/ -x`
  </how-to-verify>
  <verify>
    <automated>cd /Users/criox4/Codes/yt-to-skill && python -m pytest tests/ -x</automated>
  </verify>
  <done>Human has verified keyframe extraction on real trading videos. Threshold produces 5-15 distinct chart screenshots per video. No keyframe explosion. Gallery section renders correctly in SKILL.md.</done>
  <resume-signal>Type "approved" if keyframe quality looks good, or describe issues (e.g., "too many frames", "all frames look the same", "threshold needs adjustment to X")</resume-signal>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x` -- full suite passes
- `python -c "from yt_to_skill.stages.skill import render_gallery_section; print(render_gallery_section([]))"` -- returns empty string
- `python -c "from yt_to_skill.cli import main"` -- CLI imports without error
- Manual: run on a real video and verify SKILL.md has gallery + assets/ has PNGs
</verification>

<success_criteria>
- SKILL.md files include ## Chart References gallery when keyframes present
- Gallery omitted when no keyframes (backward compatible)
- Orchestrator runs keyframe stage after skill stage, copies PNGs, re-renders SKILL.md
- CLI --no-keyframes and --max-keyframes flags functional
- AdaptiveDetector threshold produces reasonable results on real trading videos (verified by human)
- Full test suite green with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-visual-enrichment/03-02-SUMMARY.md`
</output>
